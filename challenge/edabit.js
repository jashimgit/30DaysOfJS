/*
problem 1: find the index (part 2)

Create a function that searches
for the index of a given item in an array.If the item is present, it should return the index, otherwise, it should
return -1.

search( [ 1, 2, 3, 4 ], 3 )➞ 2
*/

const arr = [ 5, 12, 8, 130, 44 ];


function search(arr, item) {
    const c = arr.indexOf('')
}

search(arr, 4);
